apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: shared-auth-nfs
  namespace: default
  annotations:
    helm.sh/resource-policy: keep
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: synology-nfs
